ALDAR OFFLINE FINAL - MANUAL PACKAGE/CLUSTER DROPDOWN UPDATE

What is changed in this version:
1. Contractor auto-fills as TAJV and weak OCR cannot change it to wrong text.
2. Inspector Name auto-fills as Javed Iqbal and weak OCR cannot change it to wrong text.
3. Package is now a dropdown with only 2 and 4.
4. Cluster is now a dropdown linked to selected package:
   Package 2: 7A, 7B, 8A, 8B, 8C, 8D
   Package 4: 1A, 1B, 2A, 2B, 2C
5. If OCR detects package/cluster correctly it will auto-select them.
6. If OCR cannot detect package/cluster, user can select manually from controlled lists.
7. No OpenAI API key or payment required.

Use:
- Open index.html or upload files to GitHub Pages.
- Upload photo.
- Review contractor, inspector, package and cluster.
- Update Excel.
