ALDAR FINAL KPI EXCEL UPDATER - OPTION A + OPTION B

What is fixed:
1. Option A: enhanced photo OCR with preprocessing and smart score extraction.
2. Option B: manual review/correction remains available before updating Excel.
3. Excel formatting is now preserved by copying the exact style ID from the previous/template row for every updated cell.
4. No new Excel style is forced on the row, so background color, text color, font, borders and alignment match the existing record.
5. Month, week, package, cluster, KEO/role, and KPI scores are written to the same mapped columns.
6. The workbook is not rebuilt; only Weekly_Inspections sheet XML and shared strings are updated.

How to use:
1. Open index.html.
2. Upload the KPI screenshot/photo.
3. Click Option A: Auto Read Photo.
4. Review all values in the website fields and KPI boxes.
5. Correct any value manually if required.
6. Upload the original Excel if needed, or use the included original.xlsx.
7. Click Option B: Review + Update Excel.

Important:
OCR depends on image clarity. If the photo is not clear, Option B manual correction is the safe final control.
