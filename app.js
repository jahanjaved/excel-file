const HEADERS = [
  "Inspection_ID","Inspection_Date","Week_No",null,"Month","Package","Cluster","Package_Cluster_Check","Contractor","Project Director (PD)","Project Manager (PM)","Construction Manager (CM)","HSE Manager (HSEM)","Section Engineer (SE)","Your_Name","Your_Role","Area_or_Villa","Workers_Observed","Main_High_Risk_Activity","Stop_Work","Work_at_Height","Edge_Protection","Falling_Object_Prevention","Excavation_Safety","Scaffolding_Compliance","PTW_Implementation","PTW_Field_Verification","MSRA_Quality","Lifting/Precast Installation","Traffic_Interface","Housekeeping/Waste Management","Welfare Arrangement","Fire_Readiness","Supervision_Subcontractor","Electrical_Tool_Safety","Total_Score","Raw_Score_%","Weighted_Score_%","Critical_Red_Flag","Award_Eligible","Rating_Band","Repeat_Issue_Seen","Likely_Root_Cause","Top_3_Gaps_Observed","Immediate_Action_Taken","Preventive_Action_Required","Positive_Observations","Action_Owner","Target_Closeout_Date","Evidence_Link_or_Photo","KPI_for_PD_Attention","KPI_for_PM_Attention","KPI_for_CM_Attention","KPI_for_HSEM_Followup","KPI_for_SE_Supervision","Leadership_Visible_Effective","Unsafe_Work_Corrected_Immediately","Best_Improved_Cluster_this_Week","Cluster_Needing_Immediate_Intervention"
];
const KPI_WEIGHTS = [
  ["Work_at_Height",0.12,"Work at Height Control"],["Edge_Protection",0.12,"Edge Protection"],["Falling_Object_Prevention",0.05,"Falling Object Prevention"],["Excavation_Safety",0.08,"Excavation Safety"],["Scaffolding_Compliance",0.10,"Scaffolding Compliance"],["PTW_Implementation",0.08,"PTW Implementation"],["PTW_Field_Verification",0.06,"PTW Field Verification"],["MSRA_Quality",0.03,"MSRA Quality"],["Lifting/Precast Installation",0.10,"Lifting/Precast Installation"],["Traffic_Interface",0.06,"Traffic & Man-Machine Interface"],["Housekeeping/Waste Management",0.03,"Housekeeping/Waste Management"],["Welfare Arrangement",0.03,"Welfare Arrangement"],["Fire_Readiness",0.05,"Fire Readiness"],["Supervision_Subcontractor",0.03,"Supervision & Subcontractor Control"],["Electrical_Tool_Safety",0.06,"Temporary Electrical & Tool Safety"]
];
const KPI_ALIASES = {
  "Work_at_Height":["work at height","wah"],"Edge_Protection":["edge protection"],"Falling_Object_Prevention":["falling object","dropped object"],"Excavation_Safety":["excavation"],"Scaffolding_Compliance":["scaffolding","scaffold"],"PTW_Implementation":["ptw implementation","permit to work implementation"],"PTW_Field_Verification":["ptw field verification","field verification"],"MSRA_Quality":["msra"],"Lifting/Precast Installation":["lifting","precast"],"Traffic_Interface":["traffic","man-machine","man machine"],"Housekeeping/Waste Management":["housekeeping","waste"],"Welfare Arrangement":["welfare"],"Fire_Readiness":["fire readiness","fire"],"Supervision_Subcontractor":["supervision","subcontractor","sub-contractor"],"Electrical_Tool_Safety":["electrical","tool safety","temporary electrical"]
};
const CLUSTERS = {
  "Cluster 7A":{pkg:2, contractor:"TAJV"},"Cluster 7B":{pkg:2, contractor:"TAJV"},"Cluster 8A":{pkg:2, contractor:"TAJV"},"Cluster 8B":{pkg:2, contractor:"TAJV"},"Cluster 8C":{pkg:2, contractor:"TAJV"},"Cluster 8D":{pkg:2, contractor:"TAJV"},"Cluster 3":{pkg:3, contractor:"INNOVO"},"Cluster 4":{pkg:3, contractor:"INNOVO"},"Cluster 5":{pkg:3, contractor:"INNOVO"},"Cluster 6":{pkg:3, contractor:"INNOVO"},"Cluster 1A":{pkg:4, contractor:"TAJV"},"Cluster 1B":{pkg:4, contractor:"TAJV"},"Cluster 2A":{pkg:4, contractor:"TAJV"},"Cluster 2B":{pkg:4, contractor:"TAJV"},"Cluster 2C":{pkg:4, contractor:"TAJV"}
};
const $ = s => document.querySelector(s);
const excelEpoch = Date.UTC(1899,11,30);
function isoDateSerial(iso){ if(!iso) return ""; const d = new Date(iso+"T00:00:00"); return Math.floor((Date.UTC(d.getFullYear(),d.getMonth(),d.getDate())-excelEpoch)/86400000); }
function weekNo(iso){ if(!iso) return ""; const d = new Date(iso+"T00:00:00"); const t = new Date(Date.UTC(d.getFullYear(),d.getMonth(),d.getDate())); const n = t.getUTCDay() || 7; t.setUTCDate(t.getUTCDate()+4-n); const y = new Date(Date.UTC(t.getUTCFullYear(),0,1)); return Math.ceil((((t-y)/86400000)+1)/7); }
function addDays(iso,days){ if(!iso) return ""; const d = new Date(iso+"T00:00:00"); d.setDate(d.getDate()+days); return d.toISOString().slice(0,10); }
function setStatus(msg,tone="normal"){ const box=$("#status"); box.textContent=msg; box.style.background=tone==="error"?"#fff3f3":tone==="success"?"#f3fff7":"#fff"; }
function setOcrStatus(msg,tone="normal"){ const box=$("#ocrStatus"); box.textContent=msg; box.style.background=tone==="error"?"#fff3f3":tone==="success"?"#f3fff7":"#fff"; }
function cleanText(s){ return String(s||"").replace(/[|]/g,"I").replace(/[—–]/g,"-").replace(/\r/g,"").replace(/[ \t]{2,}/g," "); }
function normalize(s){ return cleanText(s).toLowerCase().replace(/[^a-z0-9\/.:\-\s&]/g," ").replace(/\s+/g," ").trim(); }
function extractDate(text){ const m=[...String(text).matchAll(/(\d{1,2})[\/\-.](\d{1,2})[\/\-.](20\d{2})/g)]; if(!m.length) return ""; const a=m[0]; return `${a[3]}-${a[2].padStart(2,"0")}-${a[1].padStart(2,"0")}`; }
function extractClusterPackage(text){ const n=normalize(text); let cluster="", pkg=""; const codes=["1A","1B","2A","2B","2C","7A","7B","8A","8B","8C","8D"]; for(const code of codes){ if(new RegExp(`\\b${code.toLowerCase()}\\b`).test(n) || n.includes(`cluster ${code.toLowerCase()}`)){ cluster=`Cluster ${code}`; break; } } const simple=n.match(/cluster\s*[:\-]?\s*([3456])\b/); if(!cluster && simple) cluster=`Cluster ${simple[1]}`; const p=n.match(/package\s*[:\-]?\s*([234])\b/); if(p) pkg=p[1]; if(!pkg && cluster && CLUSTERS[cluster]) pkg=String(CLUSTERS[cluster].pkg); return {cluster,pkg}; }
function normalizeScoreToken(token){ const raw=String(token||"").trim().toUpperCase().replace(/,/g,"."); if(!raw) return ""; if(/^(NA|N\/A|N\.A)$/.test(raw)) return "na"; let s=raw.replace(/[OS]/g,"0").replace(/[IL]/g,"1"); if(/^[0-5](\.0|\.5)?$/.test(s)) return String(Number(s)); const m=s.match(/^([0-5])([05])$/); if(m) return `${m[1]}.${m[2]}`; return ""; }
function scoreFromLine(line){ const tokens=String(line||"").replace(/N\s*\/\s*A/gi,"N/A").match(/N\/?A|NA|[0-5](?:[\.,][05])?|[0-5][05]/gi)||[]; for(let i=tokens.length-1;i>=0;i--){ const v=normalizeScoreToken(tokens[i]); if(v) return v; } return ""; }
function extractScores(text){ const lines=cleanText(text).split(/\n+/).map(x=>x.trim()).filter(Boolean); const scores={}; for(const [key,aliases] of Object.entries(KPI_ALIASES)){ for(let i=0;i<lines.length;i++){ const l=lines[i].toLowerCase(); if(!aliases.some(a=>l.includes(a))) continue; const v=scoreFromLine(lines[i])||scoreFromLine(lines[i+1]||"")||scoreFromLine(lines[i+2]||""); if(v){ scores[key]=v; break; } } }
  const count=Object.keys(scores).length;
  if(count<8){ const candidate=[]; for(const line of lines){ const v=scoreFromLine(line); if(v) candidate.push(v); }
    KPI_WEIGHTS.forEach((k,i)=>{ if(!scores[k[0]] && candidate[i]) scores[k[0]]=candidate[i]; });
  }
  return scores;
}
function updateDerived(){ const d=$("#inspectionDate").value; $("#weekNo").value=weekNo(d); $("#monthText").value=d?d.slice(0,7):""; if(d && !$("#targetCloseoutDate").value) $("#targetCloseoutDate").value=addDays(d,7); recalc(); }
function renderKpis(){ const grid=$("#kpiGrid"); grid.innerHTML=KPI_WEIGHTS.map((k,i)=>`<div class="kpi-card"><h3>${i+1}. ${k[2]}</h3><input class="score-input" data-key="${k[0]}" placeholder="Score from photo" /></div>`).join(""); document.querySelectorAll(".score-input").forEach(x=>x.addEventListener("input",recalc)); }
function scoreVal(v){ if(String(v).toLowerCase()==="na") return null; const n=Number(v); return Number.isFinite(n)?n:null; }
function allScores(){ const o={}; document.querySelectorAll(".score-input").forEach(x=>{ if(x.value.trim()) o[x.dataset.key]=x.value.trim(); }); return o; }
function recalc(){ const scores=allScores(); let total=0, weighted=0, count=0; for(const k of KPI_WEIGHTS){ const n=scoreVal(scores[k[0]]); if(n!==null){ total+=n; weighted+=n*k[1]; count++; } }
  const raw=count?total/75:""; const wp=count?weighted/5:""; const critical=["Work_at_Height","Excavation_Safety","Scaffolding_Compliance","PTW_Implementation","Lifting/Precast Installation"].some(k=>scoreVal(scores[k])!==null && scoreVal(scores[k])<=1);
  let band=""; if(count){ band=wp<0.4?"Critical":wp<0.6?"Poor":wp<0.75?"Needs Improvement":wp<0.9?"Good":"Excellent"; }
  $("#ratingBand").value=band;
  const lows=KPI_WEIGHTS.map(k=>({key:k[0],label:k[2],v:scoreVal(scores[k[0]])})).filter(x=>x.v!==null).sort((a,b)=>a.v-b.v).slice(0,3);
  if(lows.length && !$("#mainActivity").value) $("#mainActivity").value=lows[0].label.replace(" Control","").replace(" Compliance","");
  if(lows.length && !$("#rootCause").value) $("#rootCause").value=lows.some(x=>x.label.toLowerCase().includes("supervision"))?"Poor supervision":lows.some(x=>x.label.toLowerCase().includes("ptw"))?"PTW failure":"Poor supervision";
  if(lows.length && !$("#topGaps").value) $("#topGaps").value=lows.map((x,i)=>`${i+1}. ${x.label} score is ${x.v}, requiring immediate follow-up at site level.`).join("\n");
  if(lows.length && !$("#immediateAction").value) $("#immediateAction").value="Unsafe conditions were discussed with the responsible site team and immediate correction was requested at the work front.";
  if(lows.length && !$("#preventiveAction").value) $("#preventiveAction").value="The Construction Manager shall ensure close supervision, timely rectification, and verification of the identified KPI gaps before continuing similar activities.";
  $("#summaryBox").innerHTML=count?`<strong>Total:</strong> ${total} | <strong>Raw %:</strong> ${(raw*100).toFixed(1)}% | <strong>Weighted %:</strong> ${(wp*100).toFixed(1)}% | <strong>Critical Red Flag:</strong> ${critical?"YES":"NO"} | <strong>Band:</strong> ${band}`:"Scores will be extracted from the photo. Review before export.";
}
async function fileToBitmap(file){ if(window.createImageBitmap) return await createImageBitmap(file); return await new Promise((res,rej)=>{ const img=new Image(); img.onload=()=>res(img); img.onerror=rej; img.src=URL.createObjectURL(file); }); }
function canvasBlob(canvas){ return new Promise(res=>canvas.toBlob(res,"image/png")); }
async function variants(file){ const img=await fileToBitmap(file); const out=[]; for(const scale of [1,2,2.5]){ const c=document.createElement("canvas"); c.width=Math.round(img.width*scale); c.height=Math.round(img.height*scale); const ctx=c.getContext("2d",{willReadFrequently:true}); ctx.drawImage(img,0,0,c.width,c.height); if(scale>1){ const id=ctx.getImageData(0,0,c.width,c.height); for(let i=0;i<id.data.length;i+=4){ const g=0.299*id.data[i]+0.587*id.data[i+1]+0.114*id.data[i+2]; const v=Math.max(0,Math.min(255,(g-128)*1.45+128)); id.data[i]=id.data[i+1]=id.data[i+2]=v; } ctx.putImageData(id,0,0); } out.push(await canvasBlob(c)); } return out; }
async function readImage(){ const file=$("#imageInput").files?.[0]; if(!file){ setOcrStatus("Choose a photo first.","error"); return; } setOcrStatus("Reading photo deeply...","normal"); try{ let best={text:"",score:-1}; const vars=await variants(file); for(let i=0;i<vars.length;i++){ setOcrStatus(`OCR pass ${i+1}/${vars.length}...`,"normal"); const result=await Tesseract.recognize(vars[i],"eng",{logger:m=>{ if(m.status==="recognizing text") setOcrStatus(`OCR pass ${i+1}/${vars.length}: ${Math.round(m.progress*100)}%`); }}); const text=result.data.text||""; const s=Object.keys(extractScores(text)).length+(extractDate(text)?3:0)+(extractClusterPackage(text).cluster?3:0); if(s>best.score) best={text,score:s}; }
    const ocr=cleanText(best.text); $("#ocrText").value=ocr; const date=extractDate(ocr); if(date) $("#inspectionDate").value=date; const info=extractClusterPackage(ocr); if(info.pkg) $("#package").value=info.pkg; if(info.cluster) $("#cluster").value=info.cluster; if(!$("#areaText").value){ const m=ocr.match(/(?:villa|area|location|cluster)[\s:\-]*(.{3,60})/i); if(m) $("#areaText").value=m[0].trim(); }
    const scores=extractScores(ocr); document.querySelectorAll(".score-input").forEach(x=>{ x.value=scores[x.dataset.key]||""; });
    $("#yourName").value="Javed Iqbal"; $("#evidence").value="A site reference provided"; $("#repeatIssue").value="YES"; $("#actionOwner").value="Construction Manager"; updateDerived(); setOcrStatus(`Photo reading complete. ${Object.keys(scores).length}/15 KPI scores detected. Please review before export.`,"success"); setStatus("Review the detected fields, then generate the entry-only Excel.","success");
  }catch(e){ console.error(e); setOcrStatus("OCR failed. Please try another clearer photo.","error"); setStatus("OCR failed.","error"); }
}
function buildRow(){ const d=$("#inspectionDate").value; const cluster=$("#cluster").value.trim(); const pkg=$("#package").value.trim(); const scores=allScores(); let total=0, weighted=0, count=0; for(const k of KPI_WEIGHTS){ const n=scoreVal(scores[k[0]]); if(n!==null){ total+=n; weighted+=n*k[1]; count++; } }
  const raw=count?total/75:""; const wp=count?weighted/5:""; const critical=["Work_at_Height","Excavation_Safety","Scaffolding_Compliance","PTW_Implementation","Lifting/Precast Installation"].some(k=>scoreVal(scores[k])!==null && scoreVal(scores[k])<=1); const band=count?(wp<0.4?"Critical":wp<0.6?"Poor":wp<0.75?"Needs Improvement":wp<0.9?"Good":"Excellent"):""; const award=count?(!critical && wp>=0.75?"YES":"NO"):"";
  const row=new Array(HEADERS.length).fill(""); const set=(h,v)=>{ const i=HEADERS.indexOf(h); if(i>=0) row[i]=v; };
  set("Inspection_ID", d&&cluster?`${d.replaceAll("-","")}-${cluster.replace(/\s+/g,"")}-01`:""); set("Inspection_Date", d?isoDateSerial(d):""); set("Week_No", weekNo(d)); set("Month", d?d.slice(0,7):""); set("Package", pkg?Number(pkg):""); set("Cluster", cluster); set("Package_Cluster_Check", "CHECK"); set("Contractor", CLUSTERS[cluster]?.contractor||""); set("Your_Name", $("#yourName").value.trim()||"Javed Iqbal"); set("Your_Role", $("#yourRole").value.trim()||"KEO"); set("Area_or_Villa", $("#areaText").value.trim()); set("Main_High_Risk_Activity", $("#mainActivity").value.trim()); set("Stop_Work", $("#stopWork").value); KPI_WEIGHTS.forEach(k=>set(k[0], scores[k[0]]==="na"?"na":(scoreVal(scores[k[0]])??""))); set("Total_Score", count?total:""); set("Raw_Score_%", raw); set("Weighted_Score_%", wp); set("Critical_Red_Flag", count?(critical?"YES":"NO"):""); set("Award_Eligible", award); set("Rating_Band", band); set("Repeat_Issue_Seen", $("#repeatIssue").value); set("Likely_Root_Cause", $("#rootCause").value.trim()); set("Top_3_Gaps_Observed", $("#topGaps").value.trim()); set("Immediate_Action_Taken", $("#immediateAction").value.trim()); set("Preventive_Action_Required", $("#preventiveAction").value.trim()); set("Positive_Observations", $("#positiveObservations").value.trim()); set("Action_Owner", $("#actionOwner").value.trim()||"Construction Manager"); set("Target_Closeout_Date", $("#targetCloseoutDate").value?isoDateSerial($("#targetCloseoutDate").value):""); set("Evidence_Link_or_Photo", $("#evidence").value.trim()||"A site reference provided"); set("Leadership_Visible_Effective", ""); set("Unsafe_Work_Corrected_Immediately", ""); return row; }
function generateWorkbook(){ const row=buildRow(); const aoa=[["Weekly Inspection Entry Sheet - Google Form Ready"],["Entry-only file generated from photo. Copy row 4 values into the original master Weekly_Inspections sheet."],HEADERS,row]; const ws=XLSX.utils.aoa_to_sheet(aoa); ws["!cols"]=HEADERS.map(h=>({wch: h===null?4:Math.min(Math.max(String(h||"").length+2,10),35)})); ["B4","AW4"].forEach(a=>{ if(ws[a]){ ws[a].t="n"; ws[a].z="yyyy-mm-dd"; }}); ["AK4","AL4"].forEach(a=>{ if(ws[a]) ws[a].z="0.0%"; }); const wb=XLSX.utils.book_new(); XLSX.utils.book_append_sheet(wb,ws,"Weekly_Inspections"); const name=`Weekly_Inspection_New_Entry_Only_${$("#inspectionDate").value||"date"}.xlsx`; XLSX.writeFile(wb,name); setStatus("Entry-only Excel generated. Copy row 4 into your original master file.","success"); }
document.addEventListener("DOMContentLoaded",()=>{ renderKpis(); $("#inspectionDate").addEventListener("change",()=>{ $("#targetCloseoutDate").value=addDays($("#inspectionDate").value,7); updateDerived(); }); $("#readBtn").addEventListener("click",readImage); $("#generateBtn").addEventListener("click",generateWorkbook); $("#imageInput").addEventListener("change",e=>{ const file=e.target.files?.[0]; if(!file) return; const r=new FileReader(); r.onload=ev=>{ $("#imagePreview").src=ev.target.result; $("#imagePreview").style.display="block"; $("#imagePlaceholder").style.display="none"; setOcrStatus("Photo loaded. Reading will start automatically.","success"); setTimeout(readImage,250); }; r.readAsDataURL(file); }); updateDerived(); });
